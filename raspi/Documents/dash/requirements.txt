# Core game dependencies
pygame>=2.5.0
numpy>=1.24.0

# For GPIO control on Raspberry Pi (will be ignored on Windows)
RPi.GPIO>=0.7.1; platform_machine=="armv7l" or platform_machine=="aarch64"

# For sensor reading
adafruit-circuitpython-mcp3xxx>=1.4.0; platform_machine=="armv7l" or platform_machine=="aarch64"
